databases:
  - name: linksheet-db
    databaseName: linksheet
    user: linksheet_user
    plan: free

services:
  # The Redis broker for Celery
  - type: redis
    name: linksheet-redis
    plan: free
    ipAllowList: []

  # The main Django Web App + Celery Worker
  - type: web
    name: linksheet-web
    env: python
    plan: free
    buildCommand: "./build.sh"
    # Runs Celery in the background (&) and Gunicorn in the foreground
    startCommand: "celery -A config worker -l info & gunicorn config.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: linksheet-db
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: linksheet-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        value: django-db
      # You will populate these values manually in the Render dashboard
      - key: DJANGO_SECRET_KEY
        sync: false
      - key: FIELD_ENCRYPTION_KEY
        sync: false
      - key: GOOGLE_REDIRECT_URI
        sync: false
      - key: DJANGO_DEBUG
        value: "False"


